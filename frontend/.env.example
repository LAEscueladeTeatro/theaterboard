# Variables de entorno para la build de producción de Vite
# Copia este archivo a .env.production.local (o .env.production) y reemplaza con la URL real de tu backend desplegado.
# Asegúrate de que este archivo NO se suba a tu repositorio si contiene información sensible (aunque VITE_ variables son embebidas en el build).

VITE_API_BASE_URL=https://your-production-backend-url.onrender.com/api
